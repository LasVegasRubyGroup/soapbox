!!!
%html
  %head
    %title Las Vegas Ruby User Group
    = stylesheet_link_tag "application", :media => "all"
    = csrf_meta_tags
    %meta{:name => "viewport", :content => 'width=device-width, initial-scale=1.0'}
    %link{ :rel => "apple-touch-icon-precomposed", :href => "/assets/apple-touch-icon-iphone.png" }
    %link{ :rel => "apple-touch-icon-precomposed", :size => '72x72', :href => "/assets/apple-touch-icon-ipad.png" }
    %link{ :rel => "apple-touch-icon-precomposed", :size => '114x114', :href => "/assets/apple-touch-icon-iphone4.png" }

  %body
    .container
      .row
        = render 'shared/header'
        = render 'topics/sub_nav'
        / = render 'shared/alerts'
        = bootstrap_flash
        = kudos_prompt
        .alert.alert-danger
          %center 
            %h4 Heads up!
            %p OAuth with Meetup is current broken so you can't log in at the moment. We think this is on Meetup's side.

        = yield
      %footer
        A Project by the Las Vegas Ruby User Group
        |
        = mail_to('topics_app@lvrug.orgg', 'Give Us Feedback')
        |
        = link_to('Leaderboard', leaderboard_path)
        |
        = link_to('Source Code', 'https://github.com/LasVegasRubyGroup/soapbox', target: :new)

    = javascript_include_tag "application" 

    :javascript
      var _gaq = _gaq || [];
      _gaq.push(['_setAccount', 'UA-36518064-1']);
      _gaq.push(['_setDomainName', 'lvrug.org']);
      _gaq.push(['_trackPageview']);

      (function() {
        var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
        ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
      })();
